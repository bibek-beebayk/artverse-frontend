{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Information about an Artverse AI user",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "Unique identifier for the user" },
        "email": { "type": "string", "format": "email", "description": "User's email address" },
        "displayName": { "type": "string", "description": "User's display name" },
        "photoURL": { "type": "string", "format": "uri", "description": "URL to the user's profile photo" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the profile was created" }
      },
      "required": ["uid", "email"]
    },
    "FavoriteArtwork": {
      "title": "FavoriteArtwork",
      "description": "An artwork saved to a user's personal collection",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "UID of the user who favorited the artwork" },
        "artworkId": { "type": "string", "description": "ID of the favorited artwork from constants" },
        "artworkTitle": { "type": "string", "description": "Title of the artwork at the time of favoriting" },
        "artworkImageUrl": { "type": "string", "format": "uri", "description": "Image URL of the artwork" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When the artwork was favorited" }
      },
      "required": ["userId", "artworkId", "artworkTitle", "artworkImageUrl", "createdAt"]
    },
    "GeneratedArtwork": {
      "title": "GeneratedArtwork",
      "description": "An AI artwork generated by a user",
      "type": "object",
      "properties": {
        "userId": { "type": "string", "description": "UID of the generator" },
        "prompt": { "type": "string", "description": "The prompt used for generation" },
        "imageUrl": { "type": "string", "description": "Base64 encoded image data" },
        "createdAt": { "type": "string", "format": "date-time", "description": "When generated" }
      },
      "required": ["userId", "prompt", "imageUrl", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Private user profile documents"
    },
    "/users/{userId}/favorites/{artworkId}": {
      "schema": "FavoriteArtwork",
      "description": "Collection of favorite artworks for a specific user"
    },
    "/users/{userId}/generated/{artworkId}": {
      "schema": "GeneratedArtwork",
      "description": "Collection of AI generated artworks for a specific user"
    }
  }
}
